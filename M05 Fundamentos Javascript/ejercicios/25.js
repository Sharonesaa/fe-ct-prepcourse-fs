function obtenerResto(x, y) {
  // La función recibe dos argumentos "x" e "y" que son números.
  // Obten el resto de la división de "x" entre "y".
  // Tu código:
  return x % y;
}

resultado = obtenerResto();
console.log(resultado)

module.exports = obtenerResto;